<?php

namespace app\models;

use Yii;

/**
 * This is the model class for table "userform".
 *
 * @property int $id
 * @property int $form_id
 * @property string $user_name
 *
 * @property Formanswer[] $formanswers
 * @property Formtable $form
 */
class Userform extends \yii\db\ActiveRecord
{
    /**
     * @inheritdoc
     */
    public static function tableName()
    {
        return 'userform';
    }

    /**
     * @inheritdoc
     */
    public function rules()
    {
        return [
            [['form_id'], 'integer'],
            [['user_name'], 'string', 'max' => 255],
            [['form_id'], 'exist', 'skipOnError' => true, 'targetClass' => Formtable::className(), 'targetAttribute' => ['form_id' => 'id']],
        ];
    }

    /**
     * @inheritdoc
     */
    public function attributeLabels()
    {
        return [
            'id' => 'ID',
            'form_id' => 'Form ID',
            'user_name' => 'User Name',
        ];
    }

    /**
     * @return \yii\db\ActiveQuery
     */
    public function getFormanswers()
    {
        return $this->hasMany(Formanswer::className(), ['userform_id' => 'id']);
    }

    /**
     * @return \yii\db\ActiveQuery
     */
    public function getForm()
    {
        return $this->hasOne(Formtable::className(), ['id' => 'form_id']);
    }

    public function beforeDelete()
    {
        $models = Formanswer::find()->where(['userform_id'=>$this->id])->all();
        foreach ($models as $model){
            $model->delete();
        }
        return parent::beforeDelete(); // TODO: Change the autogenerated stub
    }
}
